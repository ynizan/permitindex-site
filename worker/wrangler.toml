name = "permitindex-feedback-proxy"
main = "feedback-proxy.js"
compatibility_date = "2024-01-01"

# Worker will be available at: permitindex-feedback-proxy.your-subdomain.workers.dev
# Or you can bind it to a route like: api.permitindex.com/feedback

# Environment variables (add via Cloudflare dashboard or wrangler)
# GITHUB_TOKEN = "your-github-personal-access-token"

# Optional: Custom routes
# routes = [
#   { pattern = "permitindex.com/api/feedback", zone_name = "permitindex.com" }
# ]
